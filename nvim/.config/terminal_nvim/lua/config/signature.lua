require("lsp_signature").setup({
    bind = true,
    fix_pos = true,
    hi_parameter = 'IncSearch',
    always_trigger = true,
    hint_enable = true,
    -- TODO i had this set to false idk why
})

